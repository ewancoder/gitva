- the inspector width - editable, and by default slightly more
  - maybe also visually highlight the SHA (border around it)
- adjust inspector notes / messages / hints, as well as messages toolbar hints - manually
  - MAKE LOCALIZATION: split all text into separate files first, review & adjust it, then add more languages
- make sure when git gc deletes objects that are present in recording - recording doesn't break, just shows them in RED (missing) lol
- make it a bit smaller zoom still shows labels on links
- when multiple same content blobs on the same directory level - do not one on another labels, side my side or under each other
- make it mobile-friendly somehow: snappable elements that are nicely aligning and scoped views for a bunch of objects disregarding anything else
- also in scope of mobile friendly: the inspector panel takes too much space horizontally, we need to make it collapsible or at least editable
  - or even movable below - so that actually the panel realigns itself to be horizontal rather than vertical, AND is attached to the botttom (or to the top) of the page
    - possibly it's a good idea to make EACH panel snappable / configurable position


